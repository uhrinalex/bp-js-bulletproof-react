/** @typedef {import('@/components/Form/FieldWrapper.jsx').FieldWrapperPassThroughProps} */

/**
 * @template {import('@storybook/addons').Args} TProps
 * @typedef {import('@storybook/react').Story<TProps>} Story
 */

/** @typedef {import('@storybook/react').Meta} Meta */

/** @typedef {import('@storybook/react').Story} NonGenericStory */

/** @typedef {import('react-router-dom').LinkProps} LinkProps */

/** @typedef {import('react-hook-form').FieldError} FieldError */

/** @typedef {import('react-hook-form').UseFormRegisterReturn} UseFormRegisterReturn */


/**
 * @template TFormValues
 * @typedef {import('react-hook-form').UseFormReturn<TFormValues>} UseFormReturn */

/**
* @template TFormValues
* @typedef {import('react-hook-form').SubmitHandler<TFormValues>} SubmitHandler
*/

/**
 * @template TFormValues
 * @typedef {import('react-hook-form').UseFormProps<TFormValues>} UseFormProps
 */

/**
 * @template {any} Any
 * @template ZodTypeDef
 * @template {unknown} Unknown
 * @typedef {import('zod').ZodType<any, ZodTypeDef, unknown>} ZodType
 */

/** @typedef {import('zod').ZodTypeDef} ZodTypeDef */

/** @typedef {import('react-query').DefaultOptions} DefaultOptions */
